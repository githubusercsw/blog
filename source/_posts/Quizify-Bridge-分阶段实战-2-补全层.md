---
title: Quizify Bridge 分阶段实战（二）：M2 补全层——修复平台复制丢失的符号
date: 2026-09-03 20:10:00
tags: [Quizify, 剪贴板, 预处理, Markdown, 工程化]
categories: [技术实践]
toc: true
comments: true
---

# Quizify Bridge 分阶段实战（二）：M2 补全层——修复平台复制丢失的符号

> 日期：2026-09-03 · 系列第二篇

## 摘要

M1 立起了验证台，但很快暴露一个残酷事实：**蓝本（yuanbao2anki）对剪贴板文本"完全无能力"**。AI 平台的 Markdown 在复制到剪贴板时，渲染符号大量脱落——表格变成纯文本、告警块丢了 `>`、代码围栏丢语言标记、中文编号不识别、ASCII 框线图被误判成表格。M2 补全层就是剪贴板适配的"前置修复"：在结构解析之前，先把平台弄丢的符号补回来。

## 1. 问题本质：网页 DOM vs 剪贴板文本

yuanbao2anki 蓝本假设输入是**网页 DOM**（从浏览器注入脚本读 `innerText` / HTML），结构信息天然存在。但 Quizify Bridge 的输入是**用户在 AI 平台复制后粘贴的纯文本**——复制动作会触发渲染层的"降级输出"，符号大量丢失。

实测 6 份样本的蓝本识别率惨淡：样本 03 的 9 种格式 **100% 漏识别**。

## 2. 补全层架构：三组十条规则

补全层（Preprocessor）在解析管线最前端，分三组：

```
剪贴板原始文本
    │
    ▼
┌─ 组A：预分块保护（最先，防误判）─────────┐
│  ① 代码上下文感知（缩进/围栏内不判标题/表格）│
│  ② 「框」容器识别（框线字符 → 代码框）     │
├─ 组B：符号修复（恢复平台丢失）───────────┤
│  ③ 表格三形态识别（管道/空格对齐/Tab）      │
│  ④ 告警块前缀修复（[!XXX] 缺 > 补 >）     │
│  ⑤ 中文编号归一（一、二、三 → #）         │
├─ 组C：噪声剥离─────────────────────────┤
│  ⑥ 尾部 AI 提问剥离                     │
│  ……（其余补全规则）                      │
└──────────────────────────────────────┘
    │
    ▼
 补全后的文本 → 蓝本层（M3）
```

### 2.1 组A：预分块保护——先保护再判断

**代码上下文感知**：识别缩进代码/语言标记行/围栏后，块内不判 `#`/`|`。防 `# 注释` 被误判成标题、框线图被误判成表格。

**「框」容器识别**：连续 ≥2 行含框线字符（┌└│├─┤）或结构特征 → 包代码框。一条规则覆盖卡片式布局/思维导图/Mermaid/CSV/正则等 6+ 格式。这是从用户实测反馈中提炼的：AI 平台用"框"渲染 CSV、卡片、思维导图、正则表达式，形式几乎相同，复制后只剩框线字符。

### 2.2 组B：符号修复——恢复平台丢失

**表格三形态识别**（用户实测发现的关键差异）：

| 平台 | 复制后形态 | 处理 |
|------|-----------|------|
| 标准 Markdown | 管道符 `\| a \| b \|` | 保留为 MD 表格 |
| 元宝/豆包 | 空格对齐（列对齐纯文本） | 转代码框 |
| Kimi | Tab 分隔 | 转代码框 |
| 三行表头（方向/典型岗位/核心职责） | 制表符/空格 | 修复为 `\|` 表格 |

**告警块前缀修复**：行首 `[!XXX]` 无 `>` → 补 `> ` 还原 alert。这是平台复制时把 `> [!WARNING]` 的 `>` 弄丢的典型场景。

**中文编号归一**：行首「一、二、三…」+`、` → `# `（P0 优先，用户确认很常见）。AI 对话里中文编号标题极常见，蓝本不识别。

### 2.3 组C：噪声剥离

尾部 AI 提问剥离：AI 回答末尾常附"还有什么想了解的吗？"之类的引导语，不属于卡片内容，剥离避免污染卡片。

## 3. 补全层的设计原则

补全层/蓝本层的修复与结构规则**保守**——误判会拆碎结构；增强层的新能力**激进**——包代码框最多多包一层，内容无损。

这个"保守/激进"分界贯穿全项目：**越靠近输入的修复越要保守**，因为误判的代价是结构性破坏；越靠近输出的增强越可激进，因为最多是"多包一层"这种无损兜底。

## 4. 验证

- 6 份样本补全后结构识别率 ≥90%（每份样本预先标注期望块）
- 表格三形态各有单测断言
- 中文编号归一、告警块修复各有回归用例

## 5. 本阶段启示

补全层是"蓝本能力的前置条件"——没有它，再权威的蓝本规则也识别不了被剪断的符号。这也解释了为什么项目需要"三层"而非"直接移植蓝本"：**输入形态变了，适配层必须先于规则层**。

下一篇讲 M3：蓝本层的 Kotlin 移植与语法红线。

> 系列目录：[一、JVM 验证台](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-1-JVM验证台/) · [三、蓝本层](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-3-蓝本层/)
