---
title: Quizify Bridge 分阶段实战（四）：M4 增强层——蓝本没有的新能力
date: 2026-09-03 20:30:00
tags: [Quizify, tabs, alert, cloze, 公式, 增强层]
categories: [技术实践]
toc: true
comments: true
---

# Quizify Bridge 分阶段实战（四）：M4 增强层——蓝本没有的新能力

> 日期：2026-09-03 · 系列第四篇

## 摘要

蓝本解决了"标准结构怎么定"，但用户实际使用中还有蓝本覆盖不到的场景：标签页（tabs）开放模式、公式统一体系、强调词扩展映射、cloze 收窄默认开。M4 增强层就是这些"蓝本没有的新能力"的落地点。与补全/蓝本层的保守不同，增强层允许**激进**——最多多包一层，内容无损。

## 1. 增强层四件套

### 1.1 tabs 开放模式（标签页）

蓝本只对"≥2 相邻代码块"触发 tabs。但 AI 回答里常见"短词+冒号"开头的对比段落（如"方案A：…\n方案B：…"），蓝本不识别。

增强层：**任意短词+冒号 → 标签页**，统一容器处理。风险可控——"最多多包一层代码框，内容无损"，与保守原则相符。

### 1.2 公式统一体系

AI 回答中的数学公式形态混乱：块级 `$$`、行内 `$`、化学式 `\ce`。增强层把它们统一接入公式渲染体系：

| 形态 | 处理 |
|------|------|
| 块级 `$$ ... $$` | 保留块级公式 |
| 行内 `$ ... $` | 行内公式 |
| `\ce{...}` | 化学式并入公式体系 |

### 1.3 强调词扩展映射（alert）

蓝本的 alert 四类（WARNING/IMPORTANT/CAUTION/TIP）用"词边界+120 字阀"触发。但用户场景有领域词：如医学里的"禁忌"应映射 WARNING。增强层提供**扩展映射表**：

| 词 | 类型 |
|----|------|
| 建议 / 推荐 | TIP |
| 务必 / 必须 | WARNING |
| 禁止 / 不要 | CAUTION |
| 补充 / 另外 | NOTE |
| （用户可自定义领域词） | … |

这就是"规则配置化"的雏形——映射表外置，用户可以加自己的领域词。

### 1.4 cloze 收窄默认开

蓝本 fitb（填空题）默认关。但用户反馈"引号和书名号内的内容最适合做 cloze"（如"密码银行"）。增强层：**cloze 仅对引号/书名号内容默认开启**，避免过度挖空。

## 2. 分层原则的落实

```
补全层 / 蓝本层：保守（误判拆碎结构）
增强层：激进（最多多包一层，内容无损）
```

这个原则直接决定增强层的实现风格：

- tabs 开放模式：宁可多识别，不可漏识别
- 公式：识别失败最多变纯文本，不破坏上下文
- alert：词边界匹配，避免误伤普通文本

## 3. 与规则配置化（P1-2）的衔接

M4 已经为 M5 的"规则配置化"埋下伏笔：

- alert 映射表 → 外置为 `rules.toml` 可编辑配置
- tabs 触发词 → 配置化
- 语言词典 → 外置

架构上，所有增强规则参数从 `Rules.load()` 读取，形成"L1 规则单一真源"——彻底消除 rules.md / verify_p0.py / RuleValidator.kt 三副本漂移。

## 4. 验证

- 规则设计讨论 8 项定案逐条可测
- 增强层 95/95 单测全绿（M4 里程碑门）
- 四件套各自有针对性回归用例

## 5. 本阶段启示

**增强层的价值不在"功能多"，而在"边界清晰"**。蓝本权威负责标准结构，增强层负责蓝本没有的场景，二者通过分层原则隔离——激进增强永远不会破坏保守结构。这种"权威+扩展"的双层模型，是处理"标准与特例"关系的通用答案。

下一篇讲 M5 前半段：引擎入库与 UI 三件套。

> 系列目录：[三、蓝本层](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-3-蓝本层/) · [五、引擎入库与 UI](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-5-引擎入库与UI/)
