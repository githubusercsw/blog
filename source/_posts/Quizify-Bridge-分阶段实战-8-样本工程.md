---
title: Quizify Bridge 分阶段实战（八）：样本工程——双轨对比驱动的规则迭代
date: 2026-09-03 21:10:00
tags: [Quizify, 样本工程, 双轨对比, 测试, 规则迭代]
categories: [技术实践]
toc: true
comments: true
---

# Quizify Bridge 分阶段实战（八）：样本工程——双轨对比驱动的规则迭代

> 日期：2026-09-03 · 系列第八篇

## 摘要

规则不是设计出来的，是**喂出来的**。标题识别规则体系迭代了四轮（P0-P3），每一次迭代都由真实样本驱动：脚本侧批量跑规则，语义侧人工标注理想结构，双轨对比暴露差距，再把差距沉淀成规则或测试。本文讲这套"样本工程"方法——它让规则迭代从"凭感觉"变成"可度量"。

## 1. 为什么需要样本工程

规则系统最大的风险是**过拟合与漏识别并存**：

- 只盯着一个样本调规则，换个样本就崩
- 没有基线度量，"改了没改好"只能靠感觉

解法：建立样本资产 + 双轨对比机制。

## 2. 样本资产

### 2.1 三批样本

| 批次 | 来源 | 样本 | 用途 |
|------|------|------|------|
| 第一批 | 用户真实复制 | sample-01~06（六种 AI 平台格式） | 金标回归基线 |
| 第二批 | 用户真实复制 | sample-09~18（对话类） | 批处理矩阵 |
| 自产批次 | Agent 生成 | auto-q01~q10（生活+技术问答）+ 2 篇真实文章 | 规则边界覆盖 |

### 2.2 样本形态覆盖

- 表格三形态（管道/空格对齐/Tab）
- 告警块丢 `>`、代码围栏丢语言标记
- ASCII 框线图（卡片/思维导图/Mermaid/CSV/正则）
- emoji 标题、§N 章节、第X轮/步、原则N
- 紧凑排版（标题后无空行）

## 3. 双轨对比机制

### 3.1 两条轨道

```
脚本侧（FleetAutoTest）
  ├─ 批量跑 StructureInference + Pipeline
  ├─ 输出：每样本折叠数、标题清单、门禁违规
  └─ 秒级完成，可全量回归

语义侧（人工/Agent 标注）
  ├─ 逐样本标注意想结构（哪行该是标题、折叠层级）
  └─ 形成"理想答案"
```

### 3.2 对比矩阵

| 样本 | 脚本折叠数 | 语义期望 | 差异 | 结论 |
|------|-----------|---------|------|------|
| auto-q01 | 7 | 7 | 0 | ✅ 对齐 |
| auto-q03 | 2 | 5 | -3 | 🔴 缺「步」识别 |
| auto-02（真实文章） | 0 | 10+ | -10 | 🔴 紧凑排版局限 |
| auto-q10 | 11 | 8 | +3 | 🟡 细粒度差异 |

差异分析 → 归类（规则缺口 / 粒度选择 / 局限声明）→ 决策（修规则 / 记局限 / 留 LLM 兜底）。

## 4. 规则迭代闭环

```
双轨对比 → 定位缺口 → 写失败测试（先红）
→ 修规则 → 测试转绿 → 全量回归（防回退）
→ 沉淀经验 → 下一轮
```

### 4.1 实证迭代案例

| 轮次 | 样本实证 | 修复 |
|------|---------|------|
| P1 | sample-09「第一轮：上下文工程」 | ROUND_HEAD 补轮次 |
| P1 | auto-q03「第一步：先定总预算」 | ROUND_HEAD 补「步」 |
| P2 | sample-11 引导句「两个主流技术选型：」 | 尾冒号先于疑问式排除 |
| P3 | sample-13「🤖 AGENT CONTEXT ENGINEERING — MACHINE-SPECIFICATION v1.0」44 字 | emoji 放宽到 60 + 截断 |
| P3 | sample-08「🌍 为什么国内看似"不常见"」 | emoji 含引号+疑问词放行 |

### 4.2 批次闭环纪律

用户明确批评过"协作效率太低"，沉淀为批次纪律：

1. **处理一批，沉淀一批**（避免上下文压缩丢失）
2. **问题-规则-测试一一对应**（每个规则缺口有对应失败测试）
3. **全量双轨对比**（不抽样、不凭感觉）
4. **结果落盘**（analysis/24-29 文档 + 记忆系统联动）

## 5. 已知局限（诚实声明）

- **紧凑排版零折叠**（auto-02 真实文章）：标题后无空行时弱信号不识别。保守不破规则（放宽孤立段要求会大幅误判），记为 LLM 兜底接口输入
- **细粒度 vs 语义粗粒度**（auto-q02/q04/q08/q09/q10）：脚本按形态拆块可能多于语义合并——差异属粒度选择，非缺陷

## 6. 本阶段启示

**规则迭代的最高效路径不是"想更多规则"，而是"喂更多样本"**。双轨对比把"规则好不好"变成可量化指标（折叠数对齐率、标题命中率），驱动规则向真实分布收敛。这套方法也适用于任何"启发式规则 + 人工标注"结合的系统。

下一篇讲 M5.4：UI 细节修正与样本固化的测试思维。

> 系列目录：[七、标题识别](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-7-标题识别/) · [九、UI 细节修正](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-9-UI细节与样本固化/)
