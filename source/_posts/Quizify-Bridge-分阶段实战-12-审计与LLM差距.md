---
title: Quizify Bridge 分阶段实战（十二）：规则审计与 LLM 差距——诚实地承认能力边界
date: 2026-09-03 21:50:00
tags: [Quizify, 规则审计, LLM, 能力边界, 总结]
categories: [技术实践]
toc: true
comments: true
---

# Quizify Bridge 分阶段实战（十二）：规则审计与 LLM 差距——诚实地承认能力边界

> 日期：2026-09-03 · 系列终篇

## 摘要

规则引擎做到 139 个测试全绿后，最该做的一件事是：**停下来问一句——它到底离"人"的理解差多远？** 本系列终篇讲规则审计的方法论与结论：用双轨对比量化规则与语义理解的差距，诚实标注能力边界，并给出收敛方向（LLM 兜底接口）。这是整个项目"元认知"的收束：不仅要知道系统能做什么，更要知道它不能做什么。

## 1. 为什么审计比写代码更重要

规则迭代到后期有个危险：**测试全绿 ≠ 能力达标**。测试是自证的（自己写规则、自己写断言），如果规则本身有系统性盲区，测试会一起盲。

审计的方法：**外部基准**——用语义侧的人工标注（理想结构）与脚本侧的实际输出对比，量化差距。

## 2. 审计结论：与 LLM/人工的差距

| 场景 | 规则侧 | 人工/LLM | 差距 |
|------|--------|---------|------|
| 显式信号（序号/emoji/加粗） | ~90% | ~100% | 小 |
| 列表项 vs 标题（上下文引导） | ~60% | ~100% | **最大** |
| 隐式语义标题 | ~50% | ~95% | 大 |
| 层级归位（嵌套结构） | 无 | ~100% | 无规则实现 |

### 2.1 最大差距的根因：上下文缺失

"列表项 vs 标题"之所以差，是因为区分二者需要**看上下文**：

```
让用户去：
  ① 输入密码
  ② 确认指纹
```

"① 输入密码"是列表项还是标题？答案是列表项——因为前一行"让用户去："是引导行。规则侧虽然有"前一行尾冒号→列表项"的启发式，但覆盖不了所有引导形式（动词句引导、隐含列表等）。

### 2.2 层级归位：规则侧完全没有

"哪一行是 H1、哪一行是 H2"（嵌套层级）需要全局结构感知——规则侧目前只有"`---` 后为 H1"和"强信号为 H2"两种粗糙推断。真实的层级关系（如"方案一/二/三"是并列 H2，其下还有子项）需要语义理解。

## 3. 双轨对比的量化方法

### 3.1 折叠数对齐率

每份样本：脚本折叠数 vs 语义期望折叠数。

```
auto-q01: 7 vs 7  ✅
auto-q03: 2 vs 5  🔴 缺「步」（已修复）
auto-02:  0 vs 10 🔴 紧凑排版（记局限）
auto-q10: 11 vs 8 🟡 细粒度差异
```

### 3.2 标题命中率

脚本识别的标题中，多少与语义标注一致（含误报率）。

## 4. 诚实声明：已知边界（发布说明摘录）

- **紧凑排版局限**：真实网页文章标题后无空行时，弱信号不识别（auto-02 实证）。保守不破规则——放宽孤立段要求会大幅增加误判
- **细粒度 vs 语义粗粒度**：脚本按形态信号拆块，可能多于语义合并（auto-q02/q04/q08/q09/q10）——差异属粒度选择，非缺陷
- **列表项 vs 标题**：上下文引导场景识别率 ~60%

## 5. 收敛方向：两个 LLM 兜底接口

审计的产出不是"承认差距就完事"，而是**设计收敛路径**：

### 5.1 接口一：引导行/列表项复核

规则先给出"疑似标题"候选，LLM 复核"这个候选是标题还是列表项"——只处理规则置信度低的场景，不全程依赖 LLM。

### 5.2 接口二：层级归位

规则推断出标题集合后，LLM 对标题间的层级关系（H1/H2/H3）做全局归位——利用 LLM 的全文理解能力。

**为什么是"兜底"而不是"替代"**：规则零成本、确定、可测；LLM 有成本、不确定。主路径保持规则，兜底路径只在规则薄弱处调用 LLM——这是成本与能力的平衡点。

## 6. 规则 vs LLM 的哲学

| 维度 | 规则 | LLM |
|------|------|-----|
| 成本 | 零 | 每次调用有成本 |
| 确定性 | 同一输入同一输出 | 概率性 |
| 可测性 | 完整单测覆盖 | 难以穷举 |
| 上下文感知 | 有限（行级启发式） | 全文理解 |
| 适用场景 | 高频显式形态 | 低频模糊场景 |

**结论**：不是二选一，而是**规则为主 + LLM 兜底**。这个架构在 AI 应用开发中有普遍性——凡是"高频确定 + 低频模糊"的任务，都适合这个组合。

## 7. 全系列总结

从 M1 验证台到 M6 发布，十二篇手记回顾了 Quizify Bridge 的完整历程：

```
M1 验证台    → 测试全绿为门的度量体系
M2 补全层    → 适配剪贴板输入（保守修复）
M3 蓝本层    → 权威规则 Kotlin 移植
M4 增强层    → 蓝本没有的新能力（激进扩展）
M5.1-5.2    → 引擎入库 + UI 三件套
M5.3        → 分层接线 + 标题识别
M5.4        → UI 细节 + 样本固化
M6          → 真机端到端 + 发布
```

贯穿全程的三条纪律：**测试全绿为门、环境持久化、上下文锚点**。以及一个始终如一的认知：**规则有边界，诚实标注边界，是可靠系统的一部分。**

（系列完）

> 全系列目录：
> [一、JVM 验证台](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-1-JVM验证台/) · [二、补全层](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-2-补全层/) · [三、蓝本层](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-3-蓝本层/) · [四、增强层](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-4-增强层/) · [五、引擎入库与 UI](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-5-引擎入库与UI/) · [六、分层接线](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-6-分层接线/) · [七、标题识别](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-7-标题识别/) · [八、样本工程](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-8-样本工程/) · [九、UI 细节与样本固化](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-9-UI细节与样本固化/) · [十、真机端到端](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-10-真机端到端/) · [十一、工程方法](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-11-工程方法/) · 本篇
