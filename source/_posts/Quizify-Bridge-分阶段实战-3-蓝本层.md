---
title: Quizify Bridge 分阶段实战（三）：M3 蓝本层——yuanbao2anki 权威规则的 Kotlin 移植
date: 2026-09-03 20:20:00
tags: [Quizify, 蓝本移植, Kotlin, 语法红线, 工程化]
categories: [技术实践]
toc: true
comments: true
---

# Quizify Bridge 分阶段实战（三）：M3 蓝本层——yuanbao2anki 权威规则的 Kotlin 移植

> 日期：2026-09-03 · 系列第三篇

## 摘要

补全层修好了输入，接下来要回答"结构怎么定"——答案来自蓝本：yuanbao2anki 的 markdown.js。M3 的核心是把这套经过实战检验的权威规则**逐行移植成 Kotlin**，并保留它的"语法红线"（静默退化陷阱的硬性校验）。本文讲移植方法、关键规则、以及"直通蓝本"与"增强层"的边界。

## 1. 蓝本是什么

yuanbao2anki 是 AnkiDroid 生态里把 AI 对话转成 Quizify 卡片的成熟方案，其 markdown.js 实现了从 Markdown 到 Quizify 语法的完整转换。我们的项目叫 Bridge（桥），蓝本 = 桥的"设计图纸"。

但注意：**蓝本的能力边界是网页 DOM**。上一讲已说明，纯文本剪贴板输入需要补全层先修符号，蓝本才能发挥权威性。

## 2. 移植路线：从 JS 到 Kotlin

### 2.1 蓝本的核心函数链

```
parseBlocks    → 把 Markdown 切成块（折叠/代码/表格/列表…）
mergeCodeTabs  → 相邻代码块合并为 tabs
toQuizify      → 块 → Quizify 语法
enhance        → reveal + 高亮等行内增强
```

### 2.2 移植策略

| 策略 | 内容 |
|------|------|
| 逐条断言 | 蓝本原版测试样例（11 个）转 JUnit 断言，保证行为一致 |
| 提取稿核对 | 216 行提取稿逐条断言 |
| 分层对照 | 每份样本标注"补全后 / 蓝本后 / 增强后"三态期望，防止移植偏差 |

### 2.3 蓝本规则要点（决策记录）

| 维度 | 蓝本行为 | 移植决策 |
|------|---------|---------|
| 总门槛 | 无结构信号 → 原样直出 | **采纳**：有结构信号才加工，避免机械化观感 |
| h3+ 折叠 | 不折叠原样 | **采纳**：按蓝本，只折叠 h1/h2 |
| tabs | ≥2 相邻代码块 | **采纳**：仅蓝本触发，不搞对比词启发式 |
| 列表≥2 | `> [!NOTE]` | **采纳**：列表项转提示框 |
| 表格 | 直出 | **采纳**：表格原样 |
| 强调词 Alert | 四类+词边界+120 字阀 | **采纳**：按蓝本 |
| 行内增强 | reveal + 高亮 | **采纳** |
| cloze | fitb 默认关 | **收窄默认开**：仅引号/书名号 |

## 3. 语法红线（RedlineChecker）：静默退化陷阱的硬校验

Quizify 语法有个反直觉特性：**语法错误不报错，而是静默退化**——折叠变纯文本、tabs 错乱、alert 漏内容。用户渲染出"错卡"而不自知。

RedlineChecker 就是这道防线，检查规则包括：

| 规则 | 检查内容 | 严重性 |
|------|---------|--------|
| R1 | `::::` 与 `===` 同行混用 | 致命 |
| R2 | recite 块缺 `%%` | 致命 |
| R3 | 折叠开闭不平衡（LIFO 配对） | 致命 |
| R5 | reveal 跨行 / 括号不配对 | 致命 |
| T 系列 | 静默退化提示 | 需修 |

实现上分两类：**折叠平衡**（开闭计数 + LIFO 校验）与**行内配对**（reveal/fitb 括号、recite 百分号）。

## 4. 移植中的关键坑

### 4.1 `:::` 与围栏的字面量冲突

代码示例里出现 `:::` 字面量（如展示语法本身），旧实现 `contains(":::")` 会全文误判，导致整个文档跳过转换。修复：**围栏感知扫描**——只有围栏外的 `:::` 才算结构标记。

### 4.2 嵌套折叠的 LIFO 闭合

Quizify 折叠 `:::` 是 LIFO（后进先出）配对。蓝本处理 h1→h2 嵌套时，闭合顺序必须严格逆序，否则结构错乱。这个"LIFO 闭合 + 嵌套 ≤2 层"约束贯穿后续 M5.3 的路由设计。

## 5. 验证与产出

- 蓝本 11/11 + 提取稿 216 行逐条断言全绿
- RedlineChecker 对未闭合围栏反例实测拦截
- 蓝本行为与增强层边界清晰化（蓝本权威 + 增强扩展）

## 6. 本阶段启示

**"权威规则"不等于"复制代码"**——移植过程中最花时间的不是翻译语法，而是理解每个规则背后的**假设**（输入是 DOM、h3 不折叠、列表转 NOTE…），并用测试把这些假设固化。假设不固化，下一轮迭代就漂移。

下一篇讲 M4 增强层：蓝本没有的新能力。

> 系列目录：[二、补全层](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-2-补全层/) · [四、增强层](https://chenshengwu.dpdns.org/2026/09/03/Quizify-Bridge-分阶段实战-4-增强层/)
